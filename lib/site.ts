/**
 * Канонический адрес боевого сайта — единственный источник правды для метаданных,
 * sitemap, robots и микроразметки. Раньше приложение не было привязано к домену
 * (не было canonical/sitemap), из-за чего поисковики видели «безымянный» сайт.
 */
export const SITE_URL = 'https://prokatkaterov.ru'

/** Название бренда для og:site_name и разметки. */
export const SITE_NAME = 'Судоходная компания «Дно»'

/**
 * Картинка Open Graph (og.jpg, 1200×630) — общий массив для всех страниц.
 * Важно: в Next `openGraph` страницы ЗАМЕНЯЕТ родительский целиком, поэтому
 * картинку нужно указывать в openGraph каждой страницы, а не только в layout.
 */
export const OG_IMAGES = [
  {
    url: '/og.jpg',
    width: 1200,
    height: 630,
    alt: 'Частная прогулка на катере по Неве, Санкт-Петербург',
  },
]
