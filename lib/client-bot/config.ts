/**
 * Настройки, специфичные для клиентского бота.
 */

/**
 * Лодки, скрытые ТОЛЬКО в боте (на сайте показываются как обычно).
 *
 * Поэтому не через `isVisible` — тот флаг общий с сайтом и убрал бы лодку
 * отовсюду. Здесь только слаги; фильтруется в галерее, карточке и старте брони.
 */
export const BOT_HIDDEN_SLUGS = ['formula-310ss', 'formula-206ls']
